name: CICD
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: install packages
        run: make install
      - name: Set environment variables
        env:
          DB_HOST: ids-miniproj6.crqcc88sk2vd.us-west-2.rds.amazonaws.com
          DB_NAME: miniproj6
          DB_USER: zijunw
          DB_PASSWORD: wzj010202Z!
        run: echo "Environment variables set"
      - name: transform
        run: make transform
      - name: query1
        run: make query1
      - name: query2
        run: make query2
      - name: lint
        run: make lint
      - name: test
        run: make test
      - name: format
        run: make format
      # - name: Commit files
      #   run: |
      #     git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
      #     git config --local user.name "github-actions[bot]"
      #     git add .
      #     git commit -a -m "Add changes"
      # - name: Push changes
      #   uses: ad-m/github-push-action@master
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     branch: ${{ github.ref }}